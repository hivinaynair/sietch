# Standalone workspace so `cargo test` at the repo root does not compile
# this crate with the host rustc. Built only by `cargo prove` / sp1-build.
[workspace]

[package]
name = "sietch-policy-program"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sp1-zkvm = "6.4.0"
sietch-policy-guest = { path = "../policy-guest" }
